import { GET_TRANSFER } from './actionTypes';
import { TransferBody, TransferAction } from '../types/transfers';
import { getTransfer } from '../api/transfers'

export const getTransferAction = ({ contaId,dataFim,dataInicio,nomeOperador }: TransferBody) => {

}